import { PageShell } from "@/components/arrena-shell";
export function InfoPage({ eyebrow, title, paragraphs }: { eyebrow: string; title: string; paragraphs: string[] }) { return <PageShell><article className="mx-auto min-h-[72vh] max-w-3xl px-4 py-16 sm:px-6 sm:py-24"><p className="text-xs font-semibold uppercase text-primary">{eyebrow}</p><h1 className="mt-5 max-w-2xl text-balance text-4xl font-semibold leading-tight sm:text-6xl">{title}</h1><div className="mt-12 max-w-2xl space-y-6 rounded-3xl bg-card p-6 text-base leading-8 text-muted-foreground sm:p-8">{paragraphs.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div></article></PageShell> }