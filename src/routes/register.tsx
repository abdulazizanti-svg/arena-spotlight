import { createFileRoute, Link } from "@tanstack/react-router";
import { PageShell } from "@/components/arrena-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export const Route = createFileRoute("/register")({ head: () => ({ meta: [{ title: "Create Account — ARRENA" }, { name: "description", content: "Create an ARRENA account and enter your community." }, { property: "og:title", content: "Create Account — ARRENA" }, { property: "og:description", content: "Create an account and enter your gaming community." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: () => <PageShell><div className="mx-auto min-h-[72vh] max-w-md px-4 py-20 sm:px-6"><h1 className="text-3xl font-semibold">Create account</h1><p className="mt-2 text-sm text-muted-foreground">Enter the Arena and compete for visibility.</p><form className="mt-8 space-y-4"><Input placeholder="Your name" /><Input type="email" placeholder="Email address" /><Input type="password" placeholder="Create password" /><Button className="w-full">Create account</Button></form><p className="mt-5 text-center text-xs text-muted-foreground">Already registered? <Link to="/login" className="text-foreground underline underline-offset-4">Sign in</Link></p></div></PageShell> });