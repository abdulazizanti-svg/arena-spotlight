import { createFileRoute, Link } from "@tanstack/react-router";
import { PageShell } from "@/components/arrena-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Sign In — ARRENA" }, { name: "description", content: "Sign in to manage your ARRENA communities and bids." }, { property: "og:title", content: "Sign In — ARRENA" }, { property: "og:description", content: "Manage your communities and bids." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: () => <PageShell><div className="mx-auto min-h-[72vh] max-w-md px-4 py-20 sm:px-6"><h1 className="text-3xl font-semibold">Sign in</h1><p className="mt-2 text-sm text-muted-foreground">Manage your communities and active bids.</p><form className="mt-8 space-y-4"><Input type="email" placeholder="Email address" /><Input type="password" placeholder="Password" /><Button className="w-full">Sign in</Button></form><p className="mt-5 text-center text-xs text-muted-foreground">New to ARRENA? <Link to="/register" className="text-foreground underline underline-offset-4">Create an account</Link></p></div></PageShell> });