import { createFileRoute, Link } from "@tanstack/react-router";
import { PageShell } from "@/components/arrena-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Kirish — ARRENA" }, { name: "description", content: "ARRENA hamjamiyatlari va takliflaringizni boshqarish uchun kiring." }, { property: "og:title", content: "Kirish — ARRENA" }, { property: "og:description", content: "Hamjamiyat va takliflaringizni boshqaring." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: () => <PageShell><div className="mx-auto min-h-[72vh] max-w-md px-4 py-20 sm:px-6"><h1 className="text-3xl font-semibold">Kirish</h1><p className="mt-2 text-sm text-muted-foreground">Hamjamiyatlaringiz va faol takliflaringizni boshqaring.</p><form className="mt-8 space-y-4"><Input type="email" placeholder="Elektron pochta" /><Input type="password" placeholder="Parol" /><Button className="w-full">Kirish</Button></form><p className="mt-5 text-center text-xs text-muted-foreground">ARRENAda yangimisiz? <Link to="/register" className="text-foreground underline underline-offset-4">Hisob yaratish</Link></p></div></PageShell> });